# Reader memory index

- [NE source timestamp locations](reference_ne_source_timestamp_locations.md) — patriots.com JSON-LD vs byline cross-check, two figures from one source may differ by denominator, not error, Herald HTML parsing, unregistered bylines
- [Source fetch quirks](reference_source_fetch_quirks.md) — check the byline on every outlet and treat a "Sources:" headline as possibly a relay; match seahawks.com JSON-LD by headline; ESPN box scores are unbylined; gamebook PDFs have only a CDN upload stamp

Access and blocking behaviour is not recorded here. It lives in each team's
`beat-writers/README.md` under `## Access limits` and in the `handling_note` column of that
directory's `sources.csv`. These notes cover only where metadata hides once a page is retrieved.
